---
paths:
  - "**/*.ts"
  - "**/*.tsx"
---

(guide-skill-trigger rule fired) you just read typescript. before writing or editing any:

- `.ts` → load `x:guide-typescript` (with `x:guide-code` values governing).
- `.tsx` → load BOTH `x:guide-typescript` AND `x:guide-react` — tsx is typescript; loading the
  react guide alone is the measured miss (inner-marker groom, 2026-08-25).
- about to design or change a url, route, api surface, or file layout → also
  `x:guide-conventions`.

📊 when you load a guide, say in your reply which door triggered it: «guides loaded (injected)»
if this reminder is in your context, «guides loaded (description)» if not. one clause, every
time — it is the only attribution data the fleet gets.

complementary lever only — the skill descriptions remain the primary trigger; this covers the
read-then-edit flow they sometimes miss. creating a NEW file fires nothing; the habit carries it.
