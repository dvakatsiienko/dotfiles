{
    "name": "mcp-x-cw",
    "version": "0.4.0",
    "private": true,
    "type": "module",
    "description": "Local stdio MCP server exposing CST handoffs (save/pull/delete) to cw — interop with cc via ~/.claude/shelf/handoffs/",
    "scripts": {
        "build": "tsc",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "@modelcontextprotocol/sdk": "1.30.0",
        "zod": "4.4.3"
    },
    "devDependencies": {
        "@types/node": "26.2.0",
        "typescript": "7.0.2"
    }
}
