---
paths:
  - "**/*.ts"
  - "**/*.tsx"
---

(guide-skill-trigger rule fired) you just read typescript. before writing or editing any:

- any code, any language → load `x:guide-code` FIRST.
- then `.ts` → load `x:guide-typescript`.
- `.tsx` → load `x:guide-typescript` AND `x:guide-react` — tsx is typescript; loading the
  react guide alone is the measured miss (inner-marker groom, 2026-08-25).
- about to design or change a url, route, api surface, or file layout → also
  `x:guide-conventions`.
- then say in the visible reply which door triggered the loads, naming EVERY guide loaded:
  «guides loaded (injected): code, typescript» — this rule in context = `injected`, otherwise
  `description`. naming both is what makes a missing one visible; it is the only attribution
  data the fleet gets, and it belongs in the reply, never the thinking block.
- a guide already in context does not discharge the others — check the list above per file
  type, every time (measured 2026-08-31: one loaded guide silently satisfied the urge for two).

complementary lever only — the skill descriptions remain the primary trigger; this covers the
read-then-edit flow they sometimes miss.
