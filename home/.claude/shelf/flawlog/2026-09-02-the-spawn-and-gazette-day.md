# 2026-09-02 — the spawn and gazette day

run: cclio-memory-bridge · model: fable 5.1 · boot after halt3 ingest.

## catches

- `claude --bg … --remote-control "<brief>"` — the flag ate the brief as its label; session came up idle, rc unbound. → craft-spawning already carries «prompt before --remote-control». memory.
- `sd` mangled a replacement on first real use (nested quotes); python repaired it. → one strike for the 09-15 brew vet, no rule.
- backticks inside a double-quoted `python -c` ran `lazygit` as a shell command mid-ticket-write. → always heredoc the python, never `-c` with prose. drop (habit, not rule).
- coder wrote a new key to production Upstash before naming it. → brief line «name any prod store before the first write» added to the coder brief template in craft-spawning. memory.
- pushed bytes `main` before checking which branch held the merge: main went out with ux only, stats followed a minute later. → `git status -sb` before every push in a repo a coder shares. drop (one-off, caught in the same minute).
