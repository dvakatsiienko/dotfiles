# 2026-09-24 · parallel-and-the-profile · run cc·20260923·frame

- a brief written against a tree that had moved: coder B's brief named parts3.py and 7 asks; by its first minute A had folded parts3 into build.py and landed 4 of the 7. cost: B's first stretch spent on «is someone live-editing my file». lesson: a second-coder brief carries «tree as of HH:MM, done: …» read at spawn time, and a shared-dir split waits for the first coder's extraction ping BEFORE the second spawn, not after (craft-spawning: the second brief names the first's files)
- both FRM-258 briefs named the coordinator as «🦉 cclio» (the rc label); the registry name is `cclio-fe` and B's first ping bounced. lesson: the brief carries the ListAgents name, read from `ListAgents` at spawn, never the rc label — a coder-brief line candidate
- fixed: a brief line that keeps «reference takes» must name which build renders them — B dropped board/desk on a conflict nobody decided and flagged it (correct move)
- good find (B): Artifact `read` without `path` dumps the whole live page (~15k tokens); `list scope=files` + `read path=` is the cheap route; the full read is only the republish gate
- (A's retro) a file-ownership split announced mid-flight: A had folded parts3 into build.py before the split arrived, so «extract 5 lines» became a restructure. same root as B's line above: the split is stated BEFORE the first edit — a two-coder brief names files at spawn, both spawns in one turn
- (A's retro) an avatar name that encodes its prop (clipboard → check → plain) cost three renames and three one-line fixes in another coder's file. lesson: a fleet asset is named by species + set only (`verifier-dalmatian-space`), the prop lives inside the file
- good: the coder self-timer (`Monitor sleep 900`) was armed 5 times in one session and never fired — dima answered inside every window. cheap, keep; the coder-brief line stands proven
- good find (A): a render → contact-sheet → send loop ran ~10 times by hand; `brand/profile/sheet.py <scene…>` would hold it if hero work continues
- relayed the CST's «trophy-sys is not git-connected on vercel» as fact into FRM-257's plan; a vercel api probe showed all seven bytes apps linked with `deploymentEnabled: false` — the claim was stale. cost: one wrong scope line, dima's correction. lesson: a CST live-state claim about an external system is probed before it reaches a ticket (method-rule-proof already says so; this is the sighting)
- (A's retro, shared index) A's staged `git rm`s were swept into B's commit because B committed without pathspecs — method-silent-failures «a bare git commit takes the whole index», second sighting today across two sessions in one checkout. the brief line «stage explicit paths» is not enough: the commit itself needs the pathspec, and the coder-brief should say `git commit -- <paths>`
- two coders in one checkout for six hours worked: ownership by file, every cross-edit announced as one line, i relayed «tree as of» facts between them. the cost was my attention as the switchboard (~40 relays); a shared scratch note (`brand/profile/LANES.md`, who owns what, last touch) would have cut half of them — candidate for craft-spawning
- deploy-watch.sh read «no app touched by cc54deed — nothing deploys» while the Deploy workflow fired 6 hooks (root package.json change marks every app) · cost: a false «nothing to watch» on a 6-app redeploy · lesson: the watcher must read the Deploy workflow log «to deploy:» line, never its own diff heuristic (coder f5205393, 2026-09-24)
- (bytes coder retro) an orphan branch pushed to a repo with vercel projects triggers a build per project — every `apps/*/vercel.json` opt-out must ride the branch · cost: 6 failed previews on the daily cap + a push freeze · lesson: fleet-hazards (vercel) line
- (bytes coder retro) visual iteration ran best in the artifact (6 hero versions in ~1 h, no commits) while 3 takes + a jar hero went to main and were replaced the same day · lesson: artifact first, main gets the pick — and one sample (one jar) before the set
- (bytes coder retro) a relay carrying a value dima named (an email) was acted on, a relayed push grant was re-confirmed — inconsistent · lesson: coder-brief says which relays count as his word
- (bytes coder retro) brief named /launches and /dashboard without saying both sit behind a login · 2 rounds · lesson: a shot url in a brief names its auth
- (bytes coder retro) same-name svgs cache 5 min on github; dima saw the old hero twice · lesson: a «readme stays true» line in bytes AGENTS (hard reload / versioned names)
- (bytes coder retro, nobody asked) proto-lab.vercel.app is someone else's app · financial has no dark theme · x-com-chat's public chat shows a «test» message · app shots retaken by hand 3× → `pnpm readme:shots` earns its weight — FRM-257 body
