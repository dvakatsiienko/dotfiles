# 2026-09-25 · diorama-morning · run cc·20260925·diorama

- boot: a chained command with an unquoted `echo ====` separator died on zsh equals-expansion, and the tail of the chain never ran (method-silent-failures names this exact shape, third sighting). the fix is a different invocation: quote separators or drop them
- boot + gremlin: two zsh traps in one morning — an unquoted `echo ====` and an unquoted `src/web/game*.tsx` glob with no match. both aborted the chain silently mid-output. the invocation fix: no separators, quote every glob in a grep path list
- fixed: the diorama commit hit the biome format job on `tsconfig.json` (the `include` array i widened yesterday) — the lefthook only formats what it is handed with `--write` off; per x:cmt the fix waits on dima
- a bare `#787` in a reply autolinks to the CURRENT repo in the Code tab — dima clicked it and landed on frame/issues/787 instead of charmbracelet/vhs#787. cost: a wrong page + his screenshot. lesson: a foreign issue is always a full markdown link (`[vhs#787](https://github.com/charmbracelet/vhs/issues/787)`); a bare `#N` is only ever the repo in view — candidate line for fleet-output-format «links and paths»
- the diorama brief (09-25 02:xx) was 11 asks across 3 repos; i took it whole instead of flagging overload (habit-pacing: «flag overload instead of absorbing it») — the art got a fraction of the attention and the night went into stage tooling. lesson: an art ask with a quality bar gets ONE scene first, finished to the bar, before the system spreads
- deploy-watch waited on «atelier deploy» — apps/atelier has no vercel project, so the diff heuristic counted a phantom app; trophy-sys was Ready at 15:45 while the watch still waited. second sighting of the FRM-261 class (the watcher trusts its own diff, never the Deploy log / vercel project list) → fold into FRM-261 at the flush
- relayed leva (a research lane's default pick for r3f) into the atelier spec without checking it against the spec's own rule «every control is a kit component»; the coder caught the conflict, dima's A/B killed it and asked «why did you suggest it». lesson: a research pick is checked against the spec's constraints before it enters the ticket — the lane answers «what is popular», never «what fits this brief»
- (BYT-103 verifier retro, #94, 3 rounds) brief bugs, concrete: `LINEAR_TOKEN` is ignored by linear 2.6.0 — `LINEAR_API_KEY` works; the verifier's first verdict posted as dima, reposted as coder · `github-token-wrap gh pr edit` fails, the wrapper already calls gh → `github-token-wrap pr edit` · lesson: fix both lines in x:coder-brief + x:verifier-brief at the flush
- (verifier retro) exit lines: one that names a single example string needs a «for any» twin (`xfasdf1.00` passed, `1.3xf` did not) · «fresh clone» on a seeded worktree shares the playwright cache, so the no-browser first bake never ran · lesson: the spawn ask's exit lines get a generalisation check
- (verifier retro) the round cap counts new findings, so it fired while severity fell P1 → P2 → a narrow P2 edge · lesson: a severity floor (stop when only ≤P2 edges remain) lets a converging loop finish — verifier-brief candidate
- (verifier retro) the ci reviewer ran nothing and still found 5/5 real ones incl. two the verifier's diff-read missed; the verifier found the 7 that needed running. rounds 4–7 min, reviewer 1–2 min. the pair earns its place — note for the review-stack vet
- (verifier retro) by hand every round: 4 scenes × day/night render, the bad string on every spinbutton, hmr + pixel diff, curl probes → an `atelier:verify` script or a browser test (BYT-103 follow-up candidate)
- (BYT-103 coder retro) ⌘K blanked the studio for ~1 h of dima's test drive: kit CommandDialog wired without its <Command> root, no error boundary; typecheck and tests cannot see it · lesson: every overlay is opened once in the browser before its commit — coder-brief line
- (coder retro) the number-field rule took 3 rounds because the ticket gave one example (`xfasdf1.00`), not the rule («an edit that ends invalid restores the last confirmed value») · lesson: a spec states the rule, the example illustrates it — same as the verifier's «for any» twin
- (coder retro) an unmeasured inference («pmndrs shifts colours») reached dima as a reason; the pair measured Δ 2/255 · lesson: cheap measurement before the claim, or the claim goes out labelled
- (coder retro) headless cannot see :focus-visible after a click — a stale focus ring read to dima as a selection bug, two rounds of his attention · lesson: a visual state is checked the way his eye sees it, not by aria-pressed
- (coder retro) automation candidate: `atelier:probe <piece>` — open headless, print data-rendered + console errors (typed ~15×); pairs with the verifier's `atelier:verify` idea → one BYT follow-up
- my own re-check: headless chrome ignores ⌘A, so two repro replays appended text and «failed» — selecting via `el.select()` fixed the instrument. a null from my own tooling was checked first (method-report-verify)
- #94: dima was never added as reviewer — the brief puts «add dima» on the coordinator after the verifier's `clean`; the verifier stopped at its cap instead, and i did not run the step on the cap path. dima had no approve banner and asked. lesson: a cap stop ends like a `clean` for the handoff — coordinator adds dima as reviewer in the same turn (craft-spawning / verifier-brief line)
- (BYT-104 verifier retro, #96) an exit line naming «any bake» was ambiguous (canvas vs viewer) and became dima's decision · lesson: the spawn ask's exit lines name the surface, not the object — same class as the «for any» twin from BYT-103
- (BYT-104 verifier retro) «every control reached by tab» is loose for a toggle group; the verifier read it as the ARIA pattern (tab to the group, arrows inside) · lesson: write the ARIA pattern into DESIGN.md's keyboard rule
- (BYT-104 verifier retro) the ci reviewer and the verifier again caught disjoint defects (a guessed CHROME_HEIGHT vs a − key ×0.5 / − button ÷1.5 mismatch) — second pr in a row where the pair beats either alone; review-stack vet line
- (BYT-104 verifier retro) repeated by hand: a pinch probe (synthetic ctrl-wheel + transform read), a favicon href decode, a take-panel «in view» probe → recipe candidates for x:browser-headless; the coder's scratch ATELIER_TAKES_DIR kept bake checks off the real takes → a standing line in atelier's AGENTS.md for verifiers
- (BYT-104 verifier addendum) a gesture probe via page-script dispatchEvent bypasses chrome's input path — it cannot catch an input bug; CDP `Input.dispatchMouseEvent` with fractional ctrl deltas can. and read the point under the pointer on EVERY wheel event, never between bursts — that is what caught the round-4 regression · → x:browser-headless recipe: cdp-wheel.mjs + the in-page recorder (source: the BYT-104 verifier's job tmp, alive while it lives)
- (verifier addendum) scope grew twice mid-review (canvas pinch, bounds) and each growth counted as a new finding against the 3-round cap · lesson: a scope growth resets the round count — verifier-brief line, beside the severity floor
- (BYT-104 coder retro) zoom took 5 rounds: anchor, bounds, settle and page-scroll rules were each right alone and wrong together, added one per round · lesson: a gesture spec is written as ONE rule-set test file before the first verifier round
- (coder retro + mine) two rounds were spent on dima's reports from :5180 (main, v1) while the pr ran on :5190 · lesson: an ask to dima to test a pr carries the port link, and a dima report is read for its url before it reaches a coder — cclio's filter, and a build badge in the app header (proposed)
- (coder retro) the spec's «plain scroll pans» fought the rzpp demo dima liked; linking the demo in the ticket on day one would have saved a decision round · lesson: a visual taste reference goes into the ticket as a link the moment dima names it
- (coder retro) the CDP wheel/pinch helper (~25 lines, Input.dispatchMouseEvent mouseWheel + modifiers) is rebuilt by every agent testing gestures → documented helper in x:browser-headless (second vote, with the verifier's)
- (BYT-105 coder retro) agent-browser `network route --status` filters requests, it does not set a status — the failure probe sent a 200 · lesson: mock a failing endpoint with `--abort`, or a real server error, never `--status` → x:browser-headless hazards line
- (BYT-105 verifier retro) a standing list ticket gives the verifier no exit source — one exit line per list item in the body · the verifier used `--no-verify` once on a never-pushed probe commit; next time hooks run · keep: a git-less PATH to force an error path, a main-shaped scratch clone to test a non-worktree checkout
