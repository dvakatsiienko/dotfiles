# flawlog 2026-09-10 — notes-sync-and-overnight-lane · run cclio-memory-bridge

- notes lane opened without fetching DOT-228 body · dima had to point at his own two todos · the 09-08 research answered both (bases over dataview; doctrine: drop) and nobody carried the verdicts to the ticket or to him — a research doc closing a ticket todo must write one line back into the ticket the same day
- coder-brief 0.11.50 commit carried `- ticket: DOT-26` — no ticket owned it, the id came from the queue line in my head · hook linked it, attachment deleted by hand · the id rule: from dima, the thread, or the branch; a queue neighbour is none of those
- obsidian official cli: two hangs in one wave (a `read` never returned, `rename` finished the work in 387 ms and never returned) · cost dima ~10 min of watching · evidence for the rule: an agent driving the cli times by observed effect (file landed) and kills the call, never waits on the process
- two parallel yt-transcript transits collided on the fixed `.work` dir (one fork's delete killed the other's meta.json) · a retry · fix: per-video work dir in the skill (`.work-$$` before the id is known) — skill edit at flush
- coder retro BYT-83 (1): a brief naming the symptom («confirm the bottom clipping at two widths») narrowed the look; four other axis faults were the real job · lesson: when an ask is about what dima SEES, brief the observable («verify the axes, fix what is wrong»), not the line
- coder retro BYT-83 (2): the overnight session's charts were never opened in a browser — four visible faults survived a night of work because the loop ended at typecheck · candidate rule for coder-brief: a chart or layout change ships with one screenshot in the pr
- coder retro BYT-83 (3): «flip the default» was a state change, not a code change — a stored value outranked the default · brief habit: name the observable
- coder retro BYT-83 (4): bash sandbox guard refuses `pnpm github:agent-token` (substring `git`) and `eval` (an agent-browser verb) · ~10 min · fleet-hazards sandbox section gets both shapes
- coder retro BYT-83 (5): greptile failed once server-side with no partial result, 151 s lost · coder-brief chain: one retry budgeted, then skip and name it
