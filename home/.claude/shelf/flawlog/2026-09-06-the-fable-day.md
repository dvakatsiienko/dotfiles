# 2026-09-06 — the fable day

## catches
- treated `/plugin marketplace add pbakaus/impeccable` as «installs a skill»; it also installs user-scope `PostToolUse` + `Stop` hooks that ran inside the frontend-design lane's session (`.impeccable/hook.cache.json`: session f5a912a7 scanned `bench-cv-frontend-design/*`, flagged `overused-font:inter`). lane 2 is impeccable-contaminated; the arena's isolation covered files, not hooks. → a plugin's hooks.json is part of the install; a clean comparison needs the competing plugin disabled per lane, or a project-scope install.
- the tweakcn colour refs in the frozen brief pulled all four lanes to green — a «ref, not spec» line does not neutralise a hex code. → round-2 brief carries no palette refs; distinctiveness is only measurable when the brief leaves the axis free.
- blamed my `pnpx skills remove -g` for 28 deleted files under bytes `.agents/skills/` on a timestamp match; dima had pruned them himself minutes earlier. → a timestamp is a correlation, not a cause; before naming myself or anyone as the actor, ask «did you touch this?» — one line, cheaper than a wrong confession.
- spawned a `--bg` coder from this desktop-born session on dima's «go spawn fable low»; its banner read «API Usage Billing» — the child inherited the Code tab env (`CLAUDE_CODE_ENTRYPOINT=claude-desktop`, `ANTHROPIC_BASE_URL`). stopped in ~40 s, no work done. → the spawn door depends on who booted cclio; craft-spawning gained the line. proof still owed: a terminal-born `--bg` banner.
- wrote «eject = `shadcn eject`» into the BYT-25 spec and the coder's contract brief from the tooling research's one-line mention of an `eject` command; a second research opened the changelog: `shadcn eject` inlines `shadcn/tailwind.css`, nothing to do with components. → a cli verb named in a summary gets its `--help`/changelog opened before it becomes a rule; same class as yesterday's five.
