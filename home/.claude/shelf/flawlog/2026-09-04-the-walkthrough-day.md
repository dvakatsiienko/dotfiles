# 2026-09-04 — the walkthrough day

run: cclio-memory-bridge · model: fable 5.1 · boot after halt5 ingest.

## catches

- `sd` mangled a multi-line shell replacement on its second real use (four `section` calls collapsed into fragments; python repaired). → strike two for the 09-15 brew vet; python for anything with `$`, `{`, quotes. drop.
- `ls` on `home/.config/git/` assumed the dir from an old hazard note (hooks dispatcher retreated 08-30) — it no longer existed, the first write failed. → check the dir before writing into a path a memory names. drop.
- relayed «gitignore handler.js, the build regenerates it» to the coder as dima's yes, reasoning from vercel.json alone; trophy-sys/CLAUDE.md + docs/deploy-history.md say vercel picks functions at clone time, before the build — the coder held on the repo memory and was right. → the answer i gave dima in chat was the same wrong inference. method-report-verify: a claim about a repo's behaviour opens that repo's CLAUDE.md first. memory candidate: craft-spawning «a relay to a coder carries the source it was read from».
- `pnpm plugin-release apply` answered «nothing to do» twice while the cache topped out at 0.11.19 and the source said 0.11.20 → 0.11.21; `claude plugin update x@x` by hand released it. → freebie: the script's «nothing since the version moved» check misses a bump whose release never ran; compare cache dir vs source version. ticket-sized, x-queue.
- estimated the board-sweep workflow at ~150k tokens; it spent 1.27M subagent tokens for 82 tickets (11 sonnet agents, 8 bodies + comments each, 35 s). → an agent that READS through a cli pays the full body per read; the estimate must be bodies × size, not agents × a flat number. memory candidate: craft-spawning cost line. drop for now, the number itself is the lesson.
- told the coder the skill-curve explanation was missing; it was in his done-comment. my read was `comments(first: 20) … nodes[-1]` — a cap, oldest-first, with no `pageInfo`. → x:pm already says «first: is a cap»; the concrete fix: `comments(last: 3)` or `orderBy` for the newest, and never call a thing absent from a capped page. drop, the rule exists — the miss was not applying it.
- wrote `cask "usage"` under the aqua5230 tap and later handed dima `brew reinstall --cask usage`; the bare token resolved to homebrew-core's `usage-app` (media atelier's system monitor), which installed as `Usage.app` over aqua's `usage.app` on the case-insensitive disk; the purge then took both. → a tapped cask is always written with its full path in the Brewfile and in any command handed over; verify with `brew info --cask <token>` (first line names the tap) before printing an install line. drop — the Brewfile comment now carries it.
- `usage` (aqua5230) wrote 8 http hooks (127.0.0.1:19847) + a `usage` key into `~/.claude/settings.json` at 18:42 and replaced the symlink with a real file; surfaced only as the mirror gate refusing the push. → sys-settings-drift already names the class; the new fact: a third-party menu-bar app is a writer too, and the SessionEnd/PostToolUse ECONNREFUSED noise is the tell. memory line added.
- the coder's two BYT-67 comments are authored as dima, not «cclio's pet»: my brief for pass 3/4 dropped the `LINEAR_TOKEN=$(pnpm -s linear-agent-token coder)` line the preflight requires. → the 09-16 identity vet counts this run as «token not used»; the brief template line stays mandatory. drop, rule exists.
