# 2026-09-15 · raycast settings pass · run cc·20260907·raycast

- coder retro (handoff shape) 1: my brief named a new field `<lane>` and no value set or default — the coder invented `pm/code/research/design` + `any`, and the renamed halt16 file may carry a wrong lane · cost ~10 min + a decision that was dima's · lesson for coder-brief: a brief adding a field names its values or its default
- coder retro 2+3: «rename through the store cli» named a verb the cli lacked; `formatName` is really `buildName` · `--help` + one grep before a brief line is typed (same class as the 09-14 grep criterion — third sighting)
- coder retro 4: five files named, eight needed — a new filename field's brief enumerates every WRITER, not just readers
- coder retro 5 (craft-spawning candidate): a `--bg` coder briefed to a shared main checkout is refused Edit/Write by the bg-isolation guard; bash edits work. either name `"worktree": {"bgIsolation": "none"}` in the brief or spawn it in a worktree
- coder retro 6 (dotfiles CLAUDE.md candidate): `pnpm check` is repo-wide and WRITES — reformatted docs/hotkeys/map.html; biome runs on own paths only, lefthook does staged-only at commit
