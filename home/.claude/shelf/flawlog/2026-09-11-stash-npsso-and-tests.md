# flawlog 2026-09-11 — stash-npsso-and-tests · run cclio-memory-bridge

- BYT-85 brief premise: the ticket said «10-day refresh window» and nobody multiplied it against the ~25-day npsso; the coder measured live and reframed step 1 · cost: one wrong «weeks between pastes» in front of dima · lesson: a brief that carries two lifetimes states the arithmetic; measure the live number BEFORE the build (coder retro 1, 6) — candidate line for coder-brief: «verify first when the design rests on a number»
- b2 fork read psn-api's `.d.ts` for a wire fact; the coder's one `node --env-file` call read the wire and found the non-reset · lesson: an api question is answered by the live call, the type file is a relay (coder retro 4)
- the research fork's `op` probes fired two touch-id prompts on dima's mac without a warning · cost: his hands + a false «no prompt» claim in the report · lesson: a probe that touches a credential store is announced before it runs; the fork's «no prompt» was falsified by his eye
- coder retro 2: an instrument that could not take its own reading passed typecheck, lint, 67 tests, coderabbit, greptile; matt's spec axis caught it · keep the spec axis mandatory for measurement-shaped work (brief line)
- coder retro 3: a width constraint folded two readings into one number that reads identically under countdown and reset · lesson for the ui-ux guide: never pay a layout constraint out of the payload; shorten the label
- coder retro 5: sandbox refusals ×4 (`git` substring, heredoc near `gh`, `eval`) in a worktree session, ~10 min, with the hazard file in context · proposal (rules/, needs dima): lead the sandbox section with «in a worktree, ANY gh/token command goes in a scratch script first», shapes after
- coder «nobody asked»: `npssoDeathRecord` is not behind `isAutoWriteSafe`; a local run on prod kv can pollute the lifetime history · → BYT-88 gremlins line, or a ticket
- linear api: label names cannot contain emoji, label + project descriptions cap at 255 · x:pm cheatsheet candidate
- cclio told the coder «the run conclusion misled you» from a branch-filtered `gh run list`; the coder had already fixed the finding 13 min earlier and the review run was `success` · cost: one wrong steer · good find (coder): an `issue_comment` workflow runs on the DEFAULT branch — `gh run list --branch coder/…` never shows it; poll `pulls/N/reviews` + `comments`, never the run → x:github-contrib line
- coder collapsed review → handle → add-reviewer into one minute; dima opened a pr with an unhandled finding · brief line: add-reviewer only after the ci review is handled
- good find (coder #68): the ci reviewer posts to different endpoints per round — round 1 a review + inline comment, round 2 a plain issue comment; a waiter on two endpoints reported «nothing posted» over a clean pass · rule for x:github-contrib + coder-brief: poll all three (`issues/N/comments`, `pulls/N/reviews`, `pulls/N/comments`), never infer the shape from the last round
- merge cleanup after #68 took four tool calls: the coder's untracked probe (`.verify-prod.ts`) blocked `claude rm`, then the squash-merged branch read as «carries 6 commits main lacks» and `gprune -d` held it — the 09-03 squash detection did not fire on a `claude`-managed worktree branch · fix candidates: coder-brief «leave no untracked file in the worktree at final»; gprune's squash check re-probed on a claude-managed branch
- coder #69 hung 24 min on `@greptile review` round 2: greptile EDITS its summary comment in place (updated_at moves, no new post) and its check-run «Greptile Review» completes on the head sha · a waiter on new posts never sees it · rule: greptile = check-run on head + summary `updated_at`; claude = the three endpoints → x:github-contrib + coder-brief watcher lines
- good find: «Greptile Review» is a real check-run → a required status check blocks merge until it completes, same as the claude label pr will
- coder BYT-90 retro 1: #68 merged mid-flight and the rebase produced a file that could not compile (its vitest import over #68's node:assert calls); ci caught it, one red check + force-push · rule: two coders in one repo → the second brief names the first's files, or cclio holds the merge until the second pr opens
- coder BYT-90 retro 2: my worktree `exclude` steer was dead — with `projects` the root config collects no files; the dotfiles 166 = 2 × 83 hazard is a FLAT-config property · lesson: a relayed measurement from another repo names the property that made it true
- coder BYT-90 retro 3: the assert→expect codemap turned `assert.ok` into `toBeTruthy` (~19 sites), the exact blindness the conversion existed to escape; two reviewers caught it · rule for any migration: per mapping, write what the failure prints
- coder BYT-90 retro 4: the `tsc` hoist analogy breaks on one detail — vitest is imported, tsc never is; biome's `noUndeclaredDependencies` resolves the nearest package.json → scoped exemption · BYT-91 body must list which hoisted tools appear in an `import` (vite + plugins, prisma) vs never (`@types/*`)
- coder BYT-90 retro 5 (pr 3 measurement): coderabbit 1 unique (`['default']` reporter under ci kills github annotations), standards axis 2 unique, spec axis 1 unique, greptile ×3 zero · into the measurement doc
- coder BYT-90 retro 6: the brief named deliverables but not the constraint it may break («ship only what a test exercises today») · brief line candidate
- coder BYT-90 retro 7: edited the shared biome config twice unbriefed because lint had to pass — dima's call whether «lint green» licenses a shared-package edit
- conflict: `conventions/package-json.md` repo-service order (lint, typecheck, test, check, format) vs the `package-json-shape` hook which enforced a different tail and rejected the commit — one of the two is wrong, fix at the flush
- merge monitor's «coder idle» guard was hard-wired to the BYT-85 name; on #69 it pruned the BYT-90 tree with a live coder registered — safe only because the coder had already moved to the ci-review-label worktree · fix: the guard matches any `code:` session and reads its cwd against the worktree path (live cwds list, craft-spawning) — patched the script, restart
- coder #70: pr watcher seeded `since` at arm time and was blind to greptile's two P1s posted 4 min earlier; dima saw them first · rule for the standard watcher: seed the window back (2 h) at arm, never «now» (same class as the 09-08 poll-anchor flaw)
- good find: an inline review thread is answered only via `POST /pulls/{n}/comments/{id}/replies`; a top-level `gh pr comment` does not land on the thread → x:github-contrib
- good find: a concurrency key on label PRESENCE still collides when an unrelated label is added; key on «is THIS event a review request»
- the shape-hook order change blocked a coder's commit on three manifests its pr never touched (repo-wide check at commit time) · cost: one blocked commit + a round trip · fix (coder's, right): the pre-commit runs the check on STAGED manifests only, the repo-wide sweep moves to ci — `package-json-shape` gets a paths arg, lefthook passes `{staged_files}`
- coder #72 retro 1: five «done» calls that a reviewer found hollow, one mistake — verified the test PASSES, never that it can FAIL · rule for the testing guidance (root CLAUDE.md): a test reading a rendered value is proven by DELETING its input (stylesheet, attribute, variant) and watching it go red
- coder #72 retro 2: a fix removed a property of the code it replaced (dies without a stylesheet) that nobody had written down; cost a whole extra review round · brief line: before replacing an assertion, state what the old one protected
- coder #72 retro 3: asked cclio about the blocked hook without fetching main first; the answer was a commit · brief line: check the remote before asking a question a commit could answer
- coder #72 retro 4: the coder identity was renamed mid-session and no session using it was told; every review request failed until a run log said so · rule for me: an identity/label/name swap is announced to every live coder the same minute (I renamed the label and told it; the app rename was dima's and I did not relay)
- coder #72 retro 5: «ONE real test: renders, a variant, a click» pushed a conjunctive test; the right count was five · brief behaviours, not a count
- coder #72 retro 6/7 (keep): probe a reviewer's browser claim with a throwaway test that prints the value; check where a script actually runs before applying a suggested hook
- ⚠️ the required `review` context is leaky (coder, verified): github counts a SKIPPED job as passing — #72 merged with `review=skipping`; #73 was blocked only because no run had reported. unmeasured: whether a later skipped run supersedes a red one on the same head · fix: the guard publishes its OWN check run via the api (a context only a real clean review produces), and `review` moves to its own workflow file so comment events never create skipping runs — next coder job, measured first (2 min on the next pr)
- coder #73 retro 2 / good find: `gh api --paginate` emits one array PER PAGE — `.[0]` reads the first 30; fold with `jq -s add` → fleet-hazards
- coder #73 retro 4: ten workflow items arrived one at a time across the session and re-shaped the same predicate four times · lesson for me: batch known items into one brief, hold the pr ten minutes rather than drip
- coder #73 retro 1 (keep): «decide whether it is cleaner, say why if not» produced the two best decisions — brief the justification, not the implementation
- checkpoint probe 1: 10/10 facts, dima grades 6.7 — the KEEP list paraphrased his asks, details trimmed · cost: reads machinic, detail loss possible · lesson: the checkpoint CST carries his inbox/ask lines VERBATIM (fluff included); paraphrase only my own state. → `cclio:checkpoint` step 3 line at the next bump
- gate brief premise false: «a pr editing its own workflow self-skips the claude action» — true only for a workflow already on the default branch; a NEW workflow file reviews itself. cost: none this time (found 4 real defects), lesson: state the self-skip rule with its boundary; → `x:github-contrib` line at the next bump
- coder retro #75 (gate fix), keepers: (4) `x:coder-brief` — «every gh call that WRITES wears the app» + ONE token wrapper script at job start, not one per call (six written); (5) `fleet-hazards` sandbox line is too narrow — the trigger is any heredoc whose TEXT contains `git` (`github.repository` in a yaml body fires it), 4 refusals ~10 min; (6) vercel deploys fire on `scratch/*` — the `vercel.json` gate excludes `coder/*` only → one-line ticket; (8) automation: a throwaway probe workflow against REAL data at zero reviewer cost + running a guard's jq locally against saved pr json WITH a mutation check (drop the input → zero) — the guard wants fixture tests against a saved #72 payload; (2) rule: a finding naming a mechanism → read that mechanism's source for ALL its effects
- gate hole #2 (#76, abbf98f2): `review:clean` green over six findings — the guard counts unanswered INLINE threads, the reviewer delivered everything in the sticky comment, zero inline → «clean». cost: a green that lies, again · lesson: a verdict is asserted by the reviewer (explicit marker in its comment), never inferred from an absence · fix: own brief after BYT-91 — review prompt ends with a `verdict: clean|findings` line, guard requires it
- english-buddy issue #19 filed without loading `x:writing-for-humans` (outward text under dima's name = its trigger) · cost: none, text was fine · lesson: the skill check is mechanical before ANY `gh issue create` on a foreign repo; github-contrib now names writing-for-humans next to the footer line
- «three runs on this pr → cap hit» relayed from `gh run list --workflow review.yml --limit 3` — that list spans branches (two runs were #75's); the coder counted per branch: 1 · cost: a wrong premise handed to dima for a decision · lesson: a per-pr count is `workflows/<file>/runs?branch=<head>`, never the workflow-wide list
- coder retro BYT-91 keepers: (1) a manifest survey unions `dependencies` + `devDependencies` before counting (one-field read hid three tools twice); (2) the round cap is real only if something READS the counter before each label — brief line: «check the count before every label; at 1, tell cclio» + the api call, the actor never keeps the tally from memory; (3) taxonomy by grep, never by adjacency (two optional peers filed as imports); (4) a probe runs its control first; (6) ticket: clean-clone filtered install as a ci step (four minutes, the only test of the vercel path); (7) `fleet-hazards` sandbox line → «any heredoc TEXT containing the substring git» (4 refusals again)
- coder retro BYT-91 second half: (1) after a reversal, re-read every commit in the pr that asserted something about the reversed thing (`git log --oneline` on the branch) — two of round 2's findings were stale docs from the old thesis; (2) a rule is written from the docs, never from the lockfile (react-compiler paragraph wrong); (3) **a green status answers «did this fail», never «did this run»** — three systems in one day (github skipped job, vercel skipped deploy «success», review:clean with no inline threads) → fleet-hazards line; (5) review:clean false green now measured on four rounds across two prs; (6) promote the `ghw` wrapper into `plugin-x/bin` (github-token-wrap) so no coder rebuilds it; red-first on any gating check
- BYT-90 sat In Review after both prs merged: no `Closes` line in either body, and cclio wrote «Done» into the CST without closing — the close is a verified act, never a word in a handoff; the halt board now checks every In Review ticket against its merged prs
- two flush commits (070c348, 8f5cc35) claimed a coder-brief edit that a scripted replace had silently skipped (an assertion tripped after the first file, the version bump + commit ran anyway) · cost: two hollow commit messages · lesson: a scripted multi-file edit verifies with `git diff --stat` BEFORE the commit line, or uses the Edit tool per file; never chain edit + bump + commit in one shell line
