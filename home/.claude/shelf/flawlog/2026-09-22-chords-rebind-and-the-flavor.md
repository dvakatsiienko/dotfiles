# 2026-09-22 · chords rebind, the flavor pass · mini boot, sys todo + chords

- `x:guide-react` / `x:guide-typescript` did not load on a `.tsx` edit; dima asked «did you load react guide skill?». jev routed guide-code + conventions at boot and the tsx rule never fired on the write. loaded on his catch, before the commit. the skill-trigger check is mechanical at the FIRST edit of a new file type, not at boot only
- memory says «impeccable is DISABLED globally (2026-09-14)»; `claude plugin enable` answered «already enabled». state drifted under someone else's toggle; the leaf line in `craft-spawning` is stale as a fact — it should say «check `claude plugin list` before a design lane», never assert the state. disabled again at this halt (scope: project — the global one may still be on, unverified)
- `/reload-plugins` counted 7 plugins and 58 skills but the impeccable skill never appeared in this session's list; ran its procedure by reading `SKILL.md` from the cache. works, but the reload-surfacing gap is unmeasured
- the chords writer refused a second move of the same action («appears 2 times — edit by hand»): it looked rows up by action alone, and every move leaves an ended row under the same action. the parsed-row match had the same hole. live-row match (action + key + mods, no `until`) + a test that was red first. dima found it by using the thing once
- a same-day round trip leaves `since === until` rows that never counted a press; tonight's testing wrote ~14 of them and the file was restored three times. gremlin for the chords list: a zero-length row dies instead of staying
- dima killed the monitors «because i thought they block you» and asked «why so long» / «you stuck 5 min»: a 30 s deploy poll with no heartbeat reads as a stall from his side. deploy-watch: 15 s cadence + a heartbeat line every 2 min
- a `python3` edit script with several asserts aborts all-or-nothing after biome reformatted a target; four of tonight's scripts died on the second replacement and the first had already been written. one replacement per assert, or `sd -s`; and read the on-disk text after any hook-formatted write
- `$(( $1 + 40 ))` inside an agent-browser chain came out as «missing arguments» twice; literal coordinates worked. unmeasured why — noted, not chased
- the root typecheck (commit hook) caught WebGPU types missing from the chords tsconfig while `pnpm --silent --filter chords typecheck | head` had printed nothing — a `--silent` pipe hid the failure. never grep a gate's output for errors; read its exit code
- GOOD: agent-browser drove the whole rebind loop headless — mid-drag screenshot, optimistic state, focus checks, contrast measured per tinted cap (4.77–6.07) — every claim in the reply had a measurement behind it
- dima's «is it planned? :D» over the strip, «duplicate data?», «layout jumps how to solve?», «more space between key and line» — four ui reads in a row, each right, each found by looking once. the coder's own eye missed all four while measuring other things

## afternoon · DOT-255 coder retro (ccbee7b0) + the spare pool
- a `--bg` coder booted on a day-old daemon spare without the repo's root `AGENTS.md`; a minutes-old spare loaded it (two boot attachments, same cwd, same 2.1.278). feature gates pin at first read per process, the claim reset never clears them (inferred cause). → spawn preflight (spare age → `daemon stop --keep-workers` after a throwaway probe), a digest line, a cc issue on dima's yes
- coder's own subagent wrote a half-compiled `.ts` into the repo and the repo-wide typecheck hook blocked the coder's commits for two rounds; per-path ownership does not hold against a per-repo gate → candidate fleet-hazards line: «a parallel agent in a repo with a repo-wide commit gate writes to scratch until it compiles, then moves in» (needs dima's yes, rules/)
- brief named a recipe («22 % corner mask») where it should have named the artifact to match (`handoffs.png`) — three probe rounds to find the premise wrong → craft-spawning: a visual spec names the artifact, never the recipe
- an ask phrased as a ui change («history view reads as one chain») had no such view; measuring dima's real data (7 rows, 5 zero-length) shrank it to a writer seam → brief line: measure the data before drawing
- coder recalled «start `ray develop` first» late though it read the AGENTS.md; dima caught it. an imperative about WHEN in an AGENTS.md first bullet acts at read time
- naming ruling owed to dima: `x-ray:icon`, `rayconfig:decrypt` — tail as artifact or verb?
- GOOD: coder built an anchored editor (assert once, write, assert landed), 15 uses, 2 silent no-ops caught → belongs in `plugin-x/bin/` next to `github-token-wrap`, named by `coder-brief`; dima's yes
- GOOD: «test red first» briefed for one fix, used for two, both failed for the right reason
- ⚠️ two `.rayconfig` exports sat committed in the PUBLIC dotfiles repo since 09-14/09-19 (`4f74953`, `1cf21b6`); the 09-18 one opened with `12345678` → 1,638 clipboard entries in the clear. found by the coder while decrypting. purged with filter-repo + a one-push force (branch protection toggled and restored byte-identical), exports moved to `~/Documents/raycast-backups/`, `*.rayconfig` gitignored; github still serves the dangling commit → support request is dima's. → fleet-hazards candidate: «an app export is a secrets container until proven otherwise — decrypt or inspect before it is committed; a weak passphrase is plaintext». also: the pre-purge tag failed (annotated-tag config, no -m), the reflog + a saved head file were the net; filter-repo needs `echo N |` when `.git/filter-repo/already_ran` exists
- raycast Import MERGES an export — a removed field is «no change», so the ghost hotkey survived a clean re-import (19:06 export). plan B cost a coder job for a null result; the cheaper probe would have been importing the 18:38 export with ONE visible field changed first. → freebie queued: `ghosts` ignore list in the rayconfig diff (`linear-query-wide`)
