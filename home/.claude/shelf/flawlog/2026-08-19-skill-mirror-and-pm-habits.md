# flawlog — cw·20260819·batch1 (thread c, evening)
one line per flaw: what broke · cost · lesson
- plugins search_plugins result blew token cap (77k chars) · one wasted roundtrip + host/sandbox path confusion on the dump file · lesson: grep dumps with host Grep tool, not sandbox bash
- linear CLI `issue search` doesn't exist + `list --help` prints "issue mine" usage · 2 retries · lesson: list --team X --all-states --limit 0 | grep is the search idiom
- matt-skills mirror ccli→cwrk done by hand via save_skill (14 skills, companions inlined) · ~40min, no drift detection, silent staleness on plugin updates · lesson: needs automation, folded into skill-sync p1 story
- proposed delegating save_skill to spawned cwrk children ignoring own memory (dispatch-spawn-types: start_task has NO model knob, children inherit parent model) · probe spawned a second fable-low doing the same slow 1-by-1 moves, wasted quota + a wrong DOT-168 comment needing amendment · lesson: re-read spawn-types memory BEFORE architecting delegation; cwrk spawns = same model, only Agent-tool subagents take haiku/sonnet; skill saves stay with me until better tooling
- DOT-172 created without a project · Dima had to ask, one extra round · lesson: pm habit = labels AND project AND parent at create time, not label-only; extend pm-label memory
- archived DOT-165 in the dones batch while same message ordered its reopen · unarchive + restate round · lesson: apply message-ordered state changes BEFORE bulk ops that touch the same tickets
- proposed dissolving «revamp» project off an empty `list --project` result · list hides sub-issues, revamp actually holds DOT-159/160 — wrong proposal to Dima · lesson: cli list = parents only; verify project membership via issue views or gui before any structural proposal
- DOT-167 blocking edge written as body string «⛔ BLOCKS DOT-132» instead of native relation · Dima had to correct, one round · lesson: PERM HABIT — linking/blocking in any direction = linear builtin relations, never body text
- GOOD find: skill-list reinjection after every save_skill = fattest context chunk (~35%) this session · cost of bulk installs on cwrk · lesson: mcp-served skills (DOT-165) get +1 prio — zero save_skill calls, zero reinjections; batch saves when mirroring anyway
- vikar-label idea dropped in favor of existing model labels (fable 5/opus 5) — self-descriptive, no new vocab; vikar stays a skill-only concept for now, delegation buildup continues on DOT-172
- /context not available on cwrk (ccli-only) — /explain-usage here = honest estimate, not telemetry · lesson: say so upfront when printing usage
- /context DOES work here (dima proved it, first-line local command) — my earlier "ccli-only" claim wrong · one wrong assertion round · lesson: real numbers: 451.8k/1m (45%), messages=390k (86% of spend — tool RESULTS are the hog: linear dumps, skill sources, DC output), skills only 5.6k resident, deferred-mcp 44k; my ~35% "skill-list reinjection" estimate was misattributed — reinjections live inside messages but linear/matt dumps outweigh them; lesson2: estimates get an «estimate» flag, /context gets tried FIRST
- DOT-113 model-label example logged: multi-label (opus 5 + fable 5 + sonnet 5) = «any model can fold into this»; empty = also filterable-by-anyone; labels earn their place when RESTRICTING, not describing everyone
