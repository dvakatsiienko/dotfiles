# 2026-09-05 — the design and log day

run: cclio-memory-bridge · model: fable 5.1 · boot after halt6 ingest (dima compacted the thread; SessionStart compact+resume hooks both fired).

## catches

- briefed the coder «vercel cron hits `POST /api/stats/sync`» and «use `waitUntil` from `@vercel/functions`» from memory; vercel crons are GET-only (docs), and `waitUntil` silently registers nothing outside a request context while the import doubled the bundle. the coder verified both against the docs and held. → a relayed platform fact opens the vendor doc first (method-report-verify already says source OPENED; the miss was treating my own recall as a source). drop, rule exists.
- ticket body expected «invincible 23 on 07.12»; the right figure under the gaming clock was 9 / 21 — i summed two calendar days after proposing the clock that redraws them. → run the proposed rule on the data before writing an expected number. drop.
- told dima psn-api's refresh grant rotates the token (read the TYPE, not the behaviour); the coder ran three grants: identical token, 10-day window. → a type declaring a field says nothing about what the server puts in it; behaviour claims need a call. drop, method-rule-proof already says it.
- coder: a safety branch made before a reword rebase was dragged forward by `rebase.updateRefs` (on by default since DOT-159) and stopped being a recovery point. → memory candidate, fleet-hazards git section: a safety net before a rebase is a TAG or a reflog note, never a branch, while updateRefs is on.
- BYT-74 brief said pipeworx «mirrors v4 routes, schemas differ» from a blog-post summary; the live gateway has 8 routes, no per-launch route, 4 fields per row, a 2026-only window. → a datasource swap brief opens the LIVE api (one curl per route we use), never a vendor post. drop, method-report-verify covers it.
- relayed the research agent's `promptCacheTtl: 3h` as a flip to make; the api offers 5m | 1h only, and the subscription already runs 1h. → an agent's setting name is opened in the settings reference before it reaches dima. drop, same rule.
- described `/recap` as a pre-compact digest; the doc says one-line session summary. → same class. drop.
- the day's shape: 5 of 8 catches are one failure — a claim relayed from my own recall or an agent summary without the source opened, caught by the coder or dima. method-report-verify holds the rule; the miss is applying it to MYSELF as the source. → memory candidate: one line in method-report-verify, «my own recall is a relay too».
