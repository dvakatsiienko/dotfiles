# flawlog — cw·20260819·batch1 (dpatch, thread b)

- proto spawn prompt omitted explicit dev-server auto-start → maintainer built without server; skill fixed same turn (auto-start section added, dpatch verifies in first report)
- wrong date written into DOT-39 body (2026-08-26 vs actual 08-19) → fixed same turn; date facts come from env, not vibes
- premature close: DOT-152 closed with closing word while Dima still wanted du testing → reopened+assigned; ask «anything left on this?» before closing eval tickets
- linear cli: `--assignee me` invalid (user not found) → use explicit username; note for pm skill
- project→team miss: cv project assumed DOT, lives on BYT → check project's team before create
- computer-use: devtools window steals frontmost checks, type kept failing → click target window first, expect retries; pre-open ask habit ticketed DOT-166
- blind css revs (2!) before looking at the screen → screenshot-first when ui is disputed; root cause was a setting, not css
