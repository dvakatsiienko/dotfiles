# flowlog — cw·20260819·batch1 (dpatch)

Per-session process-flaw log. One line per flaw as it happens. Wrap = final analysis + ONE
batched decision/flush. System-improvement data — check past logs when a flaw class repeats.

- handoff-then-continue waste: saved full CST, then proceeded in-session → ask «here or fresh?» BEFORE saving
- wrong model announce at open (said opus, am fable) → hard-coded in dpatch-init now
- vet recall failure: vocab lived in unloaded pm skill, claimed «not in memory» → load pm before vocab claims; recall-at-boot bug queued (worklog)
- naming violation: minted «init-dpatch», subject-first says dpatch-init → 2 repack cycles; naming check before minting artifacts
- Write tool blocked on home/.claude paths (protected) → DC write_file is the fallback; candidate announcements line
- start_code_task 180s timeout while spawn actually succeeded → verify list_sessions before retry, never double-spawn
- opus RFC spawn died 529, no retry policy declared → declare retry/fallback in multi-agent designs upfront

improvements vs 08-19-overnight audit: worklog-as-checklist → zero dropped asks; approvals batched; zero child spawns for text work.
- links habit relapse: bare/backticked ticket ids in report v2 despite standing output rule → memory file links-always-https minted, skill format rules carry it
