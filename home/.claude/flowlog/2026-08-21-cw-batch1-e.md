# flowlog — cw·20260819·batch1, thread e (opus 5, direction-change session)

- **invented ticket ids before creating the tickets.** Wrote `ticket: DOT-187` / `DOT-188` into
  research + spec frontmatter while drafting. DOT-187 already existed (unrelated: /taste-skill).
  Cost: a sed pass to correct three files, and a near-miss on shipping a wrong reference.
  Lesson: create the ticket first, THEN write its id. Never draft an id forward. This is the
  existing "never invent an id" rule failing at a place it did not obviously cover — frontmatter,
  not prose.
- **Desktop Commander rejects shell function definitions and some multi-heredoc commands.** Three
  retries lost on ticket-body batching. The rejection is not consistent by size — the same shape
  passed twice then failed. Lesson: for ticket bodies, write the file with `write_file`, then run
  the `linear` command as its own call. Never batch more than two heredocs.
- **`linear api` takes the query positionally**, not `--query`. And `linear issue list` does not
  exist — the subcommand is `issue mine`. Two help-page round-trips.
- **cross-device `mv` fails from `/tmp` into the mounted dotfiles path.** Rewrite the file in place
  instead of building it in /tmp and moving.
- **research docs carried no freshness marker until Dima asked for one.** A doc whose staleness is
  invisible is a doc that will be trusted after it goes wrong. Convention introduced this session:
  `researched:` / `sources-current-as-of:` / `refresh-when:` frontmatter. 12 older docs unbackfilled.
  Lesson: the no-timestamps-in-prose rule was over-applied — it bans dates in prose, and a freshness
  field is a date that IS the fact, which the rule explicitly carves out.
- **good catch, keep it**: checking the open-ticket list before creating caught the DOT-187
  collision and surfaced DOT-173 as a fold candidate. The pre-create placement gate (DOT-182) earns
  its keep even unimplemented.
