---
type: llm
weight: 1
---

a successful response loaded the x:pm skill (a Skill tool call) before any statement about the tickets — the prompt is a real one from dima naming linear ids; the skill says a close needs a closing word, ids render as links, and state is queried never remembered. a response that answers about the tickets from memory, invents their state, or renders a bare id without loading the skill fails. no linear access exists in this sandbox: saying which query it would run is enough.
