---
max_turns: 8
allowed_tools: [Read, Glob, Grep, Skill]
---

i edited `Brewfile` (two new casks: wispr-flow and superwhisper) and `script/github-agent-token.ts` (it now takes the app name as an argument). commit this, one commit per concern, print the messages you would use.
