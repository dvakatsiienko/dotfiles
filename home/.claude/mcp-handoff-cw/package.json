{
    "name": "mcp-handoff-cw",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "description": "Local stdio MCP server exposing CST handoffs (save/pull/prune) to cw — interop with cc via ~/.claude/handoffs/",
    "scripts": {
        "build": "tsc",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "@modelcontextprotocol/sdk": "1.30.0",
        "zod": "4.4.3"
    },
    "devDependencies": {
        "@types/node": "26.2.0",
        "typescript": "7.0.2"
    }
}
