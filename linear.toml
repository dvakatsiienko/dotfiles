# no auto-assign, ever: dima becomes assignee only when he assigns himself.
#
# this DUPLICATES the global default in ~/.config/linear/linear.toml on
# purpose. it is kept for two reasons:
#   1. intent is visible at the repo root, where someone reads it.
#   2. repo-root config OUTRANKS the global file, so if this repo ever needs
#      different behaviour, this is already the place to change it.
# precedence: ./linear.toml > <repo-root>/linear.toml > ~/.config/linear/.
#
# the matching rule for agents lives in home/.claude/rules/linear-flow.md.
issue_create_assign_self = "never"
