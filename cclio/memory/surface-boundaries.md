**an agent mutates only its own memory.** `cw` and cloud `cc` keep their own stores; neither is
yours to edit.

- 🎯 when a «surfaces are out of sync» symptom appears, never propose another sync mechanism —
  ask whether the two sides need to be two sides. the live «sync memory everywhere» instruction
  was retired for growing mess faster than coverage; the coordinator migration resolved its half
  by deletion.
- **mcp vs cli — ask which machine the shell reaches.** cclio's shell is dima's mac → prefer the
  cli. `cw`'s shell is a throwaway cloud container → a local mcp server is its only door to the
  real filesystem. say that inside the tool's description, or a `cw` thread just runs the command
  itself.
- fleet-bound facts queue on [DOT-186](https://linear.app/x-com/issue/DOT-186), even while that
  ticket is on hold — losing a fact is worse than moving it twice.
- 📌 several leaves here are fleet-wide (the pm cluster above all) sitting in a private store no
  other session sees. placement is [DOT-73](https://linear.app/x-com/issue/DOT-73) step 3 — never
  move them unilaterally.

Related: [pm](pm.md), [skill-namespaces](skill-namespaces.md)
