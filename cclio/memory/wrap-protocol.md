---
name: wrap-protocol
description: "what \"wrap\" means — missed-stuff sweep, pretty mutable report, auto-handoff, boot prompt, fun oneliner"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 9049cda0-e526-4d60-97a0-c603f6ab2782
  modified: 2026-08-19T15:28:46.214Z
---

When Dima says "wrap": (1) sweep for unclosed/missed items and remind him if needed; (2) print a pretty report — agreed shape as base, but MUTABLE: fresh flavour each wrap, start basic; (3) auto-save handoff in default full-carry mode unless he scope-steers; (4) print a copy-paste boot prompt for the fresh thread ([[session-close-boot-prompt]]); (5) end with a fun oneliner.

**Why:** agreed 2026-08-18; he plans to turn this into a proper skill later ("lets plan ask skill for you").

**How to apply:** run all five every wrap, keep report shape evolving, never skip the missed-stuff thinking pass. Wrap also includes: flowlog batched flush proposal (ONE approve), memory snapshot-sync → ~/.claude/memory-dispatch (cp + commit + push, until DOT-115), and pruning fully-done worklog buckets ([[clean-up-after-yourself]]).

## Boot prompt shape (merged from session-close-boot-prompt, 2026-08-18)

Right after the handoff save confirmation, hand Dima a ready-to-paste boot prompt for the fresh thread, as a fenced code block: (1) `/handoff-pull` line first, (2) the initial ask — first-acts from META in imperative form (anchor checks, session-specific verifications) + the session's agenda picks, (3) pretty — the output kit from [[ticket-heavy-replies-need-structure]] applies inside the prompt (bold, backticks, emojis ok). Why: classic CST — the pull command + initial ask belong together, pre-written.
