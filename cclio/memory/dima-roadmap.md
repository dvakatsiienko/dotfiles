# the roadmap — what we do, in order

Dima's plan, not an agent's. When he changes it, rewrite this file the same session and say so —
current-state document, never append-only. [[dima-strategy]] holds the branches, [[dima-stories]]
the history.

**Destination, his words:** *«a good harness setup with solid mem system, precise, clean and
working skills, not having trash, so everything is going.»* Steps 1–4 build that state; 5–7 are
what it was built for.

1. ✅ cclio migrates — [DOT-188](linear://linear.app/issue/DOT-188)
2. 🚧 **memory trashclean + fleet package rethink** — one pass. Trashclean largely done; the
   package half remains ([DOT-185](linear://linear.app/issue/DOT-185)): cclio + `cw` as one
   package, mcp vs cli decided by which machine the shell reaches.
3. **the pm milestone** — the takeover milestone on project `pm` (his insert 2026-08-26).
4. **dima's tools** — git, zsh, nvim, dotfiles. Deliberately after the fleet settles.
   [DOT-159](linear://linear.app/issue/DOT-159) remainder rides here. His order 2026-08-26:
   finish harness polish, plan git fixes + zsh fixes, then reassess. Bytes is explicitly not
   near yet.
5. **the personal cli** — one interface over the rest.
6. **BYT prettify** — the bytes tracker. ⚠️ freebies only until step 5 lands.
7. **the bytes repo prettify** — the code.
8. 🎯 **build apps** — numi, lab, and the rest. The point of all of it.

Underneath all seven: keep pm healthy and the fleet picture in view — standing duty, not a phase.

## using it

- know the step at boot; name it when reporting.
- place proposed work on this list before agreeing. Ahead-of-step work is a freebie or a ticket,
  never a session.
- the order is his, not a ranking — a later step is blocked, not lesser.
- 🚨 **pacing check:** steps 1–2 are meta-work, and meta-work never finishes on its own — bytes
  once went untouched for over a week. Step 2 closing is the moment to ask what starts next.

📌 Fable quota is scarce: Dima codes with opus Thu–Sun on the fable budget. Why fable is never
spawned.
