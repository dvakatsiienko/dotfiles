# flowlog — 2026-08-21 · run cw·20260819·batch1

- boot: `cclio/memory/` did not enter context on its own; dima had to point it out.
  cost: one turn. lesson: the barrel is ritual-read, never harness-loaded — step 2 of
  `/cclio-init` must READ it, not check for it.
- autoload solved: CLAUDE.md imports nest. paths resolve relative to the IMPORTING file
  (`@slug.md` inside the barrel), not the cwd. my first two probes failed on exactly that and
  looked like "nesting is unsupported". cost: two wasted probes. lesson: when an import loads
  nothing, suspect the path base before the feature.
- audit over-flagged `strategy-fleet` — read a conditional clause ("after the migration proves
  out, dispatch becomes a reader") as an assertion that the trial ended. dima caught it, not me.
  lesson: a subagent verdict is a candidate, never a finding. i relayed it without re-reading
  the leaf, which is the exact failure `research-vs-lived-evidence` names.
- second review round, done by hand: found 6 defects the subagent missed, incl.
  `skill-edits-are-file-edits` pointing at `cclio/.claude/skills/` — a directory that does not
  exist. lesson: an audit report is a candidate list, not a finding list. reading IS the verify
  step, and delegating it delegates the one part that cannot be delegated.
