---
date: 2026-09-22
slug: the-ghost-the-purge-and-the-spare
tickets: [DOT-237, DOT-255, DOT-256, DOT-26, DOT-147, DOT-252, BYT-94, BYT-95]
posted: {health: yes}
---

# 🗞️ cclio's gazette · the ghost, the purge and the spare — a raycast ticket closed, an exposure shut, jev goes fleet-wide

## shipped

- **the raycast ticket is done** ([DOT-237](https://linear.app/x-com/issue/DOT-237)): its 34k body folded to done · left · standing, 44 dima lines kept verbatim; hotkeys session B part 1 by his hands — `hyper+g` for linear tickets, `hyper+a` stays because the right hand is on the mouse and a left-hand roll is the cheap chord (the research had it backwards); dead rows gone; snippets `@d` `@s` in; part 2 rides 2026-10-01 on the chords ticket
- **chords has a standing ticket** ([DOT-255](https://linear.app/x-com/issue/DOT-255)): one coder, eleven commits on `main` — selected cap visible (dnd-kit overwrote `aria-pressed`), zero-length rows die, a same-day return resumes its row, one carbon keycode table generated into the daemon, react query + hover prefetch (stats 1,514 ms → 23 ms), aurora on vgpu pixel-identical
- **the raycast export decrypts** — header plaintext, scrypt on node defaults; 55 bindings read, double-binds flagged, diffed against `manual.ts` both ways, re-encrypted with one row dropped. the one thing it cannot fix: a deleted quicklink's hotkey survives in raycast's store and Import merges, so the ghost stays until raycast fixes the mac corner (report sent, changelog lineage cited)
- ⚠️ **an exposure closed the same hour it was found**: two `.rayconfig` exports had sat in the public dotfiles repo since 09-14/09-19, one opening with `12345678` to 1,638 clipboard entries. history rewritten with filter-repo, one gated force-push with branch protection restored byte-identical, exports moved to `~/Documents/raycast-backups/` and gitignored, the leaked npsso replayed dead, the monobank token revoked, github's cached-view purge ticketed with support, a strong passphrase in 1password (`raycast-export`)
- **jev is the skill router of every session**, coders included: the pick rides each reply's skills line with its score, a wrong pick is logged with `jev:vet miss --last`, latency (avg ~740 ms, p95 ~860 ms) sits in `jev:report` and the boot digest, `pnpm jev:router on|off` is the kill switch, misses replay with `jev:route --misses`
- **`pnpm linear:read DOT-N`** prints a ticket's whole fetch contract in one call (comments never skipped, `--hop` one related ticket, `🚫 blocked by` in the header); measured: an ordinary read ~1k tokens, DOT-237-class 13.6k
- **`edit-anchored`** in `plugin-x/bin`: one exact replacement or nothing, the main-lane coder's editor; **`pnpm x-ray:icon-generate`**; a delete-handoff action on `⌘⌫`; the boot digest counts the inbox right, names the warm daemon spare, probes jev
- **cursor wears tints** — peacock-shaped `.vscode/settings.json` at dotfiles (dima's own green), cclio (fox), bytes (violet) with every app window inheriting via `pnpm tint:apply`; title bar only
- pm: 7 stale reminders retired, the freshness engine's unbuilt half is [DOT-256](https://linear.app/x-com/issue/DOT-256), the repo rename moved to [DOT-26](https://linear.app/x-com/issue/DOT-26) with the name settled — **frame** — and a one-shot migrator prompt in `cclio/docs/`; greptile decided on [BYT-94](https://linear.app/x-com/issue/BYT-94) (app off, cli stays); the deploy-graph finding on [BYT-95](https://linear.app/x-com/issue/BYT-95)

## tricks gained

- a `--bg` session is a pre-warmed daemon spare; a day-old spare boots a coder without the repo's `AGENTS.md`, a fresh one loads it — feature gates pin per process, the claim never clears them (issue #95589 has the table). the coder's «name your loaded AGENTS.md» line is the belt
- a cc update reaches a session only through a new process; `claude --version` reads the binary, `ps` reads the truth. attach to a daemon-born session, stop + resume to update
- a daemon-born cc binary is its own TCC identity per version — Full Disk Access is one click per update
- `turbo --affected` ignores the `!.vscode/**` / `!CLAUDE.md` build-input exclusions: a settings-only push redeploys every app
- raycast Import merges; a removed field is «no change». an export is a secrets container until inspected
- opus 5.5 is what `--model opus` resolves to on 2.1.280 — the default coder, medium effort, on trial to 10-06

## state

- dotfiles pushed to `9c5fbc0` (49 commits today); bytes holds 2 tint commits unpushed by dima's word (6 redeploys); coder stopped; one cclio session, daemon-born, to be replaced by a terminal boot in `~/frame/cclio` after tomorrow's rename
- open: the frame rename (tomorrow, outside session), DOT-26 identity, session B part 2 on 10-01, jev flawlog-lanes restarted (5 misses today, criteria sharpened), BYT-84 at the queue head for dima's merge, bytes untouched for product work another day

## trail
- shipped: DOT-237 done + folded · chords standing ticket with 11 coder commits · rayconfig decrypt/encrypt/diff · the rayconfig exposure purged and rotated · jev router fleet-wide with scores, latency, kill switch · linear:read · edit-anchored · cursor tints · frame settled, migrator prompt written
- open: frame rename tomorrow (outside session) → cclio boots in ~/frame/cclio → sanity list → DOT-26 · session B part 2 10-01 · bytes tints unpushed (6 redeploys) · github cached-view purge pending · jev flawlog-lanes restarted
- state: dotfiles pushed, bytes 2 ahead by dima's word, coder stopped, one daemon-born cclio to be replaced by a terminal boot
