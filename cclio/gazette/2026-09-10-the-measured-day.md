---
date: 2026-09-10
slug: the-measured-day
tickets: [DOT-228, DOT-242, DOT-243, DOT-244, BYT-83, BYT-84, BYT-85, BYT-86]
posted: {health: yes}
cw: |
  the notes question got its numbers today: six ways for an agent to read and edit obsidian and notion were measured live on one suite while dima watched the files change, the icloud sync test reproduced his data loss on demand, and the recipe is now a rule for every surface (raw files and the obsidian cli, the notion cli, no mcp on either side). the first measured pull request of the review stack went through five reviewers who found one defect between them while running the app found three, and trophy-sys shipped its admin follow-ups after dima cut the token panel down three times by looking at it.
  live / next: tomorrow opens with the psn token renewing itself through 1password, then the product-map idea gets grilled — a per-app feature spec a designer can redesign from without screenshots — and the first checkpoint test runs at that topic boundary; the app pick between obsidian and notion stays dima's.
  worth a line: five code reviewers and sixty-one tests found one bug in a day's work, and opening the app in a browser at two widths found three — so the coder contract now says run it before anyone reads it.
---
# 🗞️ cclio's gazette · the measured day — six note channels on one suite, five reviewers on one pull request, the eye beats them all

## shipped

- **the notes bench ran live, six lanes, one suite** — [DOT-228](https://linear.app/x-com/issue/DOT-228) closed on it. per 50 notes: obsidian raw files 0.1 s, the official `obsidian` cli 0.4 s (the only lane that rewrites links on a rename, 231/231), the rest api every obsidian mcp wraps 1.9 s with no rename at all; notion's api at four wide 5–35 s, `ntn` 45–72 s sequential (11 s at four parallel, throttled at eight); one appended paragraph through cw's notion connector cost 23 s and 1 % of a five-hour window, through `ntn` 0.7 s and 271 bytes — and through cw's shell the same 1 %, so cw reads and the mac edits. icloud sync measured as whole-file last-writer-wins: the ipad's offline edit replaced the mac's file six minutes after reconnecting, no conflict copy. the report: [notes stack bench](https://claude.ai/code/artifact/12441837-e9d3-4b4c-b937-3d6cecbb1b8a). the recipe is binding now: `x:notes` (x 0.11.51), a notes section in root `CLAUDE.md` (dima's split), the vault section of `fleet-hazards` mirrored to cw. both mcps are gone (the notion connector off, the orphan rest-api entry removed), `ntn` came in through brew, `_router.md` gave way to a MOC with an embedded base in `_gear`. the app pick is dima's; [DOT-242](https://linear.app/x-com/issue/DOT-242) holds the obsidian sharpening plan with the two videos and the vault links; the notion free-plan odds were researched (removal under 10 % in two years, a hollowing 25–35 %, both inference).
- **review stack, pull request 1 scored** — bytes #67 / [BYT-83](https://linear.app/x-com/issue/BYT-83): 17 commits, three rounds. on the same commit before any fix: matt's `code-review` 5 true / 4 unique and free, coderabbit 1 true in 85 s, greptile 0 true across cli and ci in three rounds (2 credits, one self-withdrawn nit, one 151 s server failure); ci `@claude` died on the 30-turn cap in round 2, posted nothing, and found 2 unique defects in round 3 once the cap was 60 (bytes `a8b14eb0`). greptile did not auto-review the empty open, so no label rule. decision 2 taken: greptile is cli-only, `@greptile review` left the brief; decision 1 waits for pull request 2. the measurement moved out of the stack plan into `cclio/docs/review-stack-measurement.md` with its own dies-when. the finding above the sheet: three of the coder's eight real defects came from running the app; no reviewer found any of them.
- **trophy-sys follow-ups merged** (`f6001586`): the admin token panel is kv-only after dima cut it three times by looking — the two-source comparison, the «token in use» row, then the order (paste first) — the env var is a bootstrap seed and nothing else, with `.env.example`, two doc comments and `apps/cv/AGENTS.md` swept of the old model; the effort chart hides untouched titles by default; soundtracks and artbooks auto-hide by rule with remembered unhides; the bulk buttons got their own pending flag; four axis faults nobody had seen in a browser fixed; `financial` on the kit's `cn`; `ignoreBuildErrors` gone from all four next apps; the overnight prd refreshed to main by the coder. the overnight `bytes-b1` session's two docs (the prd, the greptile-vs-coderabbit pre-run) are tracked with dies-when. next: [BYT-85](https://linear.app/x-com/issue/BYT-85) — the npsso renews itself (1password sign-in, a refresh-grant instrument); the token died mid-use, so the 10-day refresh window does not explain it.
- **the vercel gate measured**: 51 deployments created in a day, 40 of them canceled — the `git.deploymentEnabled` gate creates then cancels, `main` pushes fan out and the built-in skip cancels five; the limits doc counts «Deployments Created per Day». the real zero is git auto-deploy off with `main` deployed from ci: [BYT-84](https://linear.app/x-com/issue/BYT-84).
- **the product map** — dima's PO question, folded: a per-app feature spec (route → view → features → states, invisible ones included) a designer redesigns from without screenshots, a wireframe generated from it, sync by one rule in the app's `CLAUDE.md`, a design lab at `~/projects/atelier`, bmad inspected for parts (its `EXPERIENCE.md` headings are the shape; the framework is not adopted): [DOT-244](https://linear.app/x-com/issue/DOT-244) → the trophy-sys map as the feature review before the redesign, [BYT-86](https://linear.app/x-com/issue/BYT-86) → BYT-75.
- **the coder contract, five bumps** (`x:coder-brief` 0.11.50 → 0.11.54): a dima mode (no ticket, no worktree, ask first), a dev server is a clickable link, run it before anyone reads it (two widths, one screenshot), stop and ask when the ui grows faster than the behaviour, removal is done when nothing teaches the old design, verify a scripted deletion's end anchor, the pr watcher ignores the vercel and linear bots, one greptile retry then skip, poll the pr comments after a ci run. root `CLAUDE.md` got a skills section (ours, maintained, load them first; name every load); [DOT-243](https://linear.app/x-com/issue/DOT-243) measures the three skill triggers; seven cursor-rules lines landed in the guides (kebab dirs dropped: PascalCase component folders stay).
- **the checkpoint** — `cclio:checkpoint` (cclio 0.3.35): a half-halt for topic switches, dima's drop list or a keep/drop proposal, a fuller CST, the `/compact` hint line; first test tomorrow with a ten-probe recall check. the halt gained the coder roster line (retro received? stopped?).

## tricks gained

- the obsidian cli hangs (a read that never returns; a rename that finished the work in 387 ms and never returned) — drive it with a timeout and judge by the file landing, never by the process
- every cli in a shell loop gets `</dev/null` — the obsidian cli ate the loop's stdin, `ntn api` hangs on an open one
- `ntn api` wants `-X PATCH` and a bare `/v1/…` path; `ntn pages trash` needs `--yes` non-interactively; the obsidian rest api answers 404 to `%2F` and takes a frontmatter patch as `Operation: replace` plus a bare json string
- notion search caps at the page_size asked; `trash` refuses icloud paths; two parallel transits need per-run work dirs
- a ci review run's «success» is not the review — the comments post after; poll them
- `git.deploymentEnabled` cancels rather than skips, and the hobby counter is «created», not «built»

## state

- dotfiles `27318be`, bytes `f6001586`, both clean on origin; no coder, no worktrees; dima's tools · next overhaul · 19/23
- tomorrow: BYT-85 first, DOT-26 with dima, the map grill (DOT-244 → BYT-86), the checkpoint test at that boundary, opus coder on BYT-81 as measurement pull request 2
- open: the obsidian-vs-notion pick (dima's), the vercel zero (BYT-84, not urgent at 51/100), DOT-242 when obsidian wins
