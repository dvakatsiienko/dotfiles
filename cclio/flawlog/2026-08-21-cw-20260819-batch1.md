# flowlog — 2026-08-21 · run cw·20260819·batch1

- boot: `cclio/memory/` did not enter context on its own; dima had to point it out.
  cost: one turn. lesson: the barrel is ritual-read, never harness-loaded — step 2 of
  `/cclio-init` must READ it, not check for it.
- autoload solved: CLAUDE.md imports nest. paths resolve relative to the IMPORTING file
  (`@slug.md` inside the barrel), not the cwd. my first two probes failed on exactly that and
  looked like "nesting is unsupported". cost: two wasted probes. lesson: when an import loads
  nothing, suspect the path base before the feature.
- audit over-flagged `strategy-fleet` — read a conditional clause ("after the migration proves
  out, dispatch becomes a reader") as an assertion that the trial ended. dima caught it, not me.
  lesson: a subagent verdict is a candidate, never a finding. i relayed it without re-reading
  the leaf, which is the exact failure `research-vs-lived-evidence` names.
- second review round, done by hand: found 6 defects the subagent missed, incl.
  `skill-edits-are-file-edits` pointing at `cclio/.claude/skills/` — a directory that does not
  exist. lesson: an audit report is a candidate list, not a finding list. reading IS the verify
  step, and delegating it delegates the one part that cannot be delegated.
- the worklog's colocation premise was partly wrong: DOT-165/168/186/118 are ALREADY children of
  DOT-28. the real defect is project≠parent-project, not missing parents. cost: none, caught by
  querying before acting. lesson: a carried-over task description is a claim, verify it first.
- DOT-184-vs-DOT-28 traced: not a dupe-search miss. two stories cut the SAME domain on DIFFERENT
  dimensions (artifact vs channel), which no pre-create search can catch. the guardrail has to be
  a dimension check, or better, a post-hoc consistency query.
- the no-assign theory (fleet commit identity) was FALSIFIED by the push test. cost: two commits
  and a rule rewrite. lesson: the theory was reasoned, plausible, and written into a binding rule
  BEFORE anyone pushed once. an inferred mechanism must be labelled inferred AND tested before it
  becomes a rule others follow.
- the linear default page cap bit twice in one session: 151-vs-80 earlier, then a project reading
  "50" when the truth was 75. lesson: any number i intend to PRINT gets pageInfo, no exceptions.
- shipped a boot query into cclio-init and only THEN tested it — it failed on linear's complexity
  cap (40132 vs 10000). caught because i tested before reporting, not before writing. lesson: for
  a command file, the test IS the write; author the query at the shell first, then paste what ran.

## flush — 2026-08-21, batched
- clusters: (a) acting on unverified claims ×4 (b) capped queries ×1, fired twice (c) ship-then-test ×1 (d) the import discovery ×2, already fully captured
- kept 2: `research-vs-lived-evidence` gains "your own inference promoted to a rule before one test";
  `skill-edits-are-file-edits` gains "author executable snippets at the shell first"
- dropped: (b) the rule already existed and is correctly worded — it failed on discipline, not wording.
  the structural fix landed anyway (boot query now derives counts client-side). (d) no gap.
