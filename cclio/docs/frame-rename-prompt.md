# the frame rename — paste into a fresh `claude --effort high` started in `~`

one-shot. dies the day the rename lands (dima 2026-09-22: «tmrw i spawn an opus in ~ and paste this»). ticket: DOT-26.

```
You are renaming my repo ~/dotfiles to frame — the directory, the github repo (dvakatsiienko/dotfiles → dvakatsiienko/frame), the remote, and every live reference on this mac and in my linear tickets. You run from ~ and never cd into the repo for the whole job (your cwd would move under you; use `git -C` and `pnpm --dir`). Read ~/dotfiles/AGENTS.md first: the mirror rule — every path under home/ IS the path under ~, linked by script/dotfiles-link.ts, and the ~/.claude and ~/.config trees are real dirs whose leaves are symlinks into this repo. Rules: nothing is destroyed — no rm, no history rewrite, every change is a rename, a re-link or a text edit; read a file fully before editing it; commit only at the end, one commit; stop and report on any surprise instead of improvising. Verify each step with the command given before the next.

0. preflight: `claude agents --json` lists no session whose cwd is under ~/dotfiles (cclio and every coder are exited — if one is alive, stop and tell me). `git -C ~/dotfiles status --short` is clean and `git -C ~/dotfiles push` says up to date. record baselines: `find ~ -maxdepth 4 -type l -lname '*dotfiles*' 2>/dev/null | wc -l` (expect ≈32 or more), `grep -l dotfiles ~/Library/LaunchAgents/*.plist` (expect 3), `jq -r '.projects|keys[]' ~/.claude.json | grep -c dotfiles` (expect 8), `ls -d ~/.claude/projects/*dotfiles*` (expect 33 dirs).
1. github + move: `gh repo rename frame -R dvakatsiienko/dotfiles` (github redirects the old name; branch protection and the linear github integration ride along — verify with `gh api repos/dvakatsiienko/frame/branches/main/protection --jq .required_status_checks.contexts` printing the check). then `mv ~/dotfiles ~/frame` and `git -C ~/frame remote set-url origin git@github.com:dvakatsiienko/frame.git`; `git -C ~/frame fetch` succeeds.
2. script family: in ~/frame/package.json every `dotfiles:*` script becomes `frame:*`; `git -C ~/frame mv script/dotfiles-link.ts script/frame-link.ts`, fix its imports, the manifest's name if it says dotfiles, and every caller (`grep -rn 'dotfiles-link\|dotfiles:' ~/frame --exclude-dir=node_modules --exclude-dir=.git`). `pnpm --dir ~/frame typecheck` exit 0.
3. re-link the mirror: `pnpm --dir ~/frame frame:link status`, then `apply`. verify: `find ~ -maxdepth 4 -type l -lname '*dotfiles*' 2>/dev/null` prints nothing, and `ls -la ~/.claude/settings.json ~/.claude/CLAUDE.md ~/.config/zsh-custom` resolve into ~/frame.
4. launchd: for each of the 3 plists that mention /Users/dima/dotfiles: `launchctl bootout gui/$UID/<label>`; edit the plist SOURCE under ~/frame (check whether ~/Library/LaunchAgents/<label>.plist is a symlink into the repo or a copy, and treat accordingly); `launchctl bootstrap gui/$UID ~/Library/LaunchAgents/<label>.plist`; `launchctl print gui/$UID/<label> | grep 'state = running'`. the hotkey daemon binary is ad-hoc signed and its Input Monitoring grant is pinned to path + hash: do NOT rebuild it; put in your report that I must re-grant it (System Settings → Privacy → Input Monitoring: remove the old row, add ~/frame/schedule/jobs/x-monitor-hotkey-stats/bin/x-monitor-hotkey-stats), and that chords will show no presses until I do.
5. claude code state (real files, not symlinks — edit with jq/python, keep valid json, back each up to ~/frame-migration-backup/ first):
   - ~/.claude.json: rename every key under .projects from /Users/dima/dotfiles… to /Users/dima/frame… (8 keys), and any other string holding the old path;
   - ~/.claude/projects/: rename the 33 transcript dirs `-Users-dima-dotfiles…` → `-Users-dima-frame…` so `claude --resume` keeps my history;
   - marketplaces: `claude plugin marketplace list`; the local ones registered under ~/dotfiles (x, cclio, possibly more) get `claude plugin marketplace remove <name>` + `add <new path>`, then `claude plugin update x@x` and, from ~/frame/cclio, `claude plugin update cclio@cclio --scope project`; `claude plugin list` shows them enabled;
   - ~/.claude/plugins/known_marketplaces.json and any cache path files that still say dotfiles;
   - settings: ~/frame/home/.claude/settings.json and ~/frame/cclio/.claude/settings.json mention the old path in hook commands — fix the strings only, and the permissions blocks must stay byte-identical (diff them before/after and say so).
6. other apps' config (real files): Claude Desktop's mcp config "~/Library/Application Support/Claude/claude_desktop_config.json" (11 mentions — the mcp-x-cw server path and args); Cursor's project manager "~/Library/Application Support/Cursor/User/globalStorage/alefragnani.project-manager/projects.json" (the dotfiles, cclio and .claude entries' rootPath; rename the `dotfiles` project to `frame`); zsh aliases in ~/frame/home/.config/zsh-custom/aliases.zsh (5 lines: cclio, cclio-list, hk, …) — keep alias names, change paths.
7. the literal sweep inside ~/frame: first every `/Users/dima/dotfiles` and `~/dotfiles` (live paths in cclio/.claude/hooks, home/.claude/shelf/hooks, plugin-x and plugin-x-cw skills — memory-sync and memory-update name the repo as the master, handoff/handoff-ingest carry the store path — script/*.ts, cclio/memory, AGENTS.md files, biome/lefthook configs, docs/knowledge); then `dotfiles` as the repo's name in prose and headings (AGENTS.md titles, readme, CONTEXT.md, plugin descriptions). LEAVE untouched: git history, cclio/gazette/*, home/.claude/shelf/flawlog/*, docs/research/* (dated records), and any quoted dima sentence. print the count of files changed.
8. linear (my tickets, use the `linear` cli — it holds my key; `linear api` for reads and `issueUpdate` mutations): `linear api 'query { searchIssues(term: "dotfiles", first: 50) { nodes { identifier title description } pageInfo { hasNextPage endCursor } } }'`, page through all. in each body replace ONLY paths and identifiers — `~/dotfiles/…`, `/Users/dima/dotfiles/…`, `dotfiles:` script names, `dvakatsiienko/dotfiles` urls (github redirects, but fix them) — never the word in prose, quotes or titles, never comments. fetch the full body, apply, and for every ticket print `DOT-N: <k> replacements` plus one sample line before/after; keep every run-stamp footer and every dima line intact. skip DOT-26 (I own that body) and any ticket in Done/Canceled state older than 30 days.
9. cw side, report only: list what `~/frame/home/.claude/plugin-x-cw/skills/memory-sync/SKILL.md` and `memory-update/SKILL.md` say about the master path after your edit, and tell me to run `/memory-sync` in Cowork once so its mirrored memory learns ~/frame.
10. done-criterion: `grep -rn dotfiles ~/frame ~/.config ~/.claude ~/.claude.json ~/Library/LaunchAgents "$HOME/Library/Application Support/Claude/claude_desktop_config.json" --exclude-dir=node_modules --exclude-dir=.git 2>/dev/null | grep -v 'gazette/\|flawlog/\|docs/research/\|projects/-Users'` prints only prose about the rename or history; `find ~ -maxdepth 4 -type l -lname '*dotfiles*'` prints nothing; `pnpm --dir ~/frame typecheck && pnpm --dir ~/frame test` exit 0; `cd ~/frame/cclio && BOOT_PROBE=1 claude -p --model haiku 'name the first heading of the project AGENTS.md you loaded'` answers with the cclio heading; `~/frame/cclio/.claude/hooks/boot-prefetch.sh` ends with `=== all checks green ===` (a FAIL line is a finding — report it, do not patch the hook).
11. commit in ~/frame, ONE commit on main, lowercase, no other trailers: subject `✨ frame: the repo is frame — dotfiles renamed on every layer`, body = one line per layer touched with counts, last line `Agent: coder · <your model name>`. push. then report: each step's proof line, the counts, the Input Monitoring re-grant I owe, the /memory-sync I owe, anything you could not do.
```

## cclio's sanity check at the first boot in `~/frame/cclio`

- the digest ends `=== all checks green ===`; `barrel probe` names `d03f3da`; the settings symlink line is OK
- `pwd` is `/Users/dima/frame/cclio`; `git remote -v` says `frame`
- `find ~ -maxdepth 4 -type l -lname '*dotfiles*'` prints nothing
- `launchctl print gui/$UID/com.dima.x-monitor-hotkey-stats | grep state` = running, and the chords board shows a press stamped after the boot (else: the Input Monitoring re-grant)
- `claude plugin list` shows `x` and `cclio` enabled; `/reload-plugins` once
- then DOT-26: the readme for the three tiers (mirror · machine · fleet), the purpose line, the license
