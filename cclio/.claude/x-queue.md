# x-queue

## queue

- 🧪 **review-stack measurement run — fires on the next 2 real coder prs (dima 2026-09-09, «important, remember all details»).** the whole plan is `cclio/docs/gh-stack-adoption.md` step 3b: three local tools on the same final commit before any fix, sheet per tool (true / noise / time / unique / credits), decision 1 = local order (coderabbit vs greptile), decision 2 = greptile cli vs ci; empty-open credit check on pr 1; brief switches to dima's fallback chain after pr 2. candidates: space-explorer-ui error boundary + cancelTrip/bookTrips auth. dima picks tasks 2026-09-10 after DOT-26.
- 🧬 **evergreen — built 2026-09-08 (DOT-240 done).** renovate tiers in bytes + dotfiles (patch daily automerge · minors monday 09:00 · majors one PR each · 3-day cooldown · 0.x never automerges · config validated in ci) · vercel gates on `renovate/*` + `coder/*` (1 preview per bot PR, measured) · bytes ci runs test + build · dotfiles has ci · branch protection + auto-merge · `x-coder-bot` · `cclio:evergreen` = the news layer. **still open:** the taste interview (his words: «grill me… pretend you interview me» — the filter; until then short lines) · the pretty pass on the report · brew weekly lane unrun · 5 renovate PRs waiting (bytes #61 postgres 18, #57 pnpm 12, #56 graphql 17; dotfiles #36 vitest 5, #35 pnpm 12 — pnpm 12 also means his global pnpm) · `cmdstats` read ~2026-09-22.
- 🥈 DOT-26 (repo identity: readme, license, purpose) — dima + cclio on fable, his word 2026-09-09: «i want you to take care of dot-26, ask me when ready». then close DOT-39 (last open child). measure day-after: an app-only merge shows 5 skipped previews and no quota move; first ci run on an unchanged package shows `cached` > 0.
- 🔧 x:coder-brief verification contract (dima's yes 2026-09-07, mostly landed in the «final» chain 0.11.47–49; remaining bit) — every done-report line pairs with the command that proved it · red→green for behaviour changes · matt's `code-review` (standards + spec) runs BEFORE the last push: review → fix → push → report «ready»; verdict line in the report · skipped for freebies / one-file PRs. the coder invokes it (opus pays, subagents stay isolated).
- 🥉 trophy-sys redesign, the map: dima's hands (DESIGN-REQUEST §10, commit the three md files) → desktop designer session → comp → impeccable builds on a worktree PR (kit primitives adopted, next-or-vite decided) → `/document` DESIGN.md closes BYT-75. then cv build (BYT-36, comp approved), outpost (BYT-76).
- 💬 **x-com-chat, in order: [BYT-81](linear://linear.app/issue/BYT-81) routes + threads + selector (measurement pr 1) → [BYT-82](linear://linear.app/issue/BYT-82) per-user threads + api guard (measurement pr 2) → [BYT-37](linear://linear.app/issue/BYT-37) instant shell.** bodies written 2026-09-09, dima promoted 81/82 to Todo; he reads them cold, steers, then the opus coder on BYT-81. before the coder: `npx skills update` in bytes (convex skills 5 months stale). tails from the audit: delete `.cursor/rules/` + root `AGENTS.md` pointer + 8 guide lines (coder day); `typescript.ignoreBuildErrors: true` in x-com-chat's next.config.
- 🏢 later, dima 2026-09-08: github org for bytes → blacksmith runners (free org; transfer bytes; re-point vercel ×6, renovate, x-coder-bot, TURBO_TEAM, remotes, linear link; blacksmith gated job in the reflog at 9c25946b). one afternoon of re-pointing, no money.
- 🎯 later: BYT-79 reuse scan (Triage) · BYT-77 payload a/b · BYT-78 storybook · DOT-228 · cclio v2 groom (0/4) · DOT-187 (now carries the non-git tool hunt) · BYT-60 name · BYT-44 grill · `@next/playwright` narrow exception when cache components land · stacked prs on the first multi-layer assignment (+1 dima 2026-09-07) · space-explorer-ui: an error boundary, api cancelTrip without validateAuth (noted on #48).
- 🧪 vets: brew picks 09-15 · coder identity 09-16 (today's comments by `coder` ✅) · initiative 09-17 · proto-lab 10-04 · `x-archive-refresh` after dima's next play.
